package com.example.crs

import androidx.fragment.app.Fragment

/**
 * Two things:
 * - outline provider options: background (default), bounds, paddedBounds, none
 * - outline ambient and spot shadow colors as a two points of light
 */
class ShadowVariantFragment : Fragment(R.layout.fragment_shadow_variant)